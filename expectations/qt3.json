{
  "skip": {},
  "xfail": {
    "ForExpr013": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "ForExpr016": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "ForExpr025": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "NodeTest001": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "NodeTest002": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "ReturnExpr005": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "ReturnExpr007": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "ReturnExpr009": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "ReturnExpr011": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "ReturnExpr015": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "XQueryComment002": "helium fn:serialize of a node selected from a larger document does not emit the in-scope namespace declarations the node inherits from its ancestors, producing an undeclared-prefix serialization that omits the expected xmlns declarations",
    "analyzeString-008": "helium fn:analyze-string does not nest captured sub-groups: nested fn:group elements are emitted as siblings, so the serialized analyze-string-result structure differs from the expected",
    "analyzeString-017a": "helium fn:analyze-string does not nest captured sub-groups: nested fn:group elements are emitted as siblings, so the serialized analyze-string-result structure differs from the expected",
    "analyzeString-020": "requires schema-validated PSVI type annotation: the built-in fn:analyze-string-result schema types @nr as xs:positiveInteger, but helium leaves the constructed attribute untyped without a compiled schema",
    "analyzeString-021": "requires schema-validated PSVI type annotation: the built-in fn:analyze-string-result schema gives the result element a named complex type, but helium leaves it xs:untyped without a compiled schema",
    "filterexpressionhc1": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "filterexpressionhc4": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "fn-collection-8": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "fn-current-dateTime-24": "helium fn:seconds-from-dateTime returns xs:double, not xs:decimal, so assert-type xs:decimal fails",
    "fn-current-time-4": "helium fn:seconds-from-time returns xs:double, not xs:decimal, so assert-type xs:decimal fails",
    "fn-doc-25": "helium fn:serialize emits the source document's DOCTYPE / internal-subset DTD declarations, which the expected assert-xml fragment omits",
    "fn-doc-26": "helium fn:serialize emits the source document's DOCTYPE / internal-subset DTD declarations, which the expected assert-xml fragment omits",
    "fn-doc-29": "helium fn:serialize emits the source document's DOCTYPE / internal-subset DTD declarations, which the expected assert-xml fragment omits",
    "fn-doc-37": "helium fn:id does not resolve the document's DTD-declared ID attribute, so the id()-selected subtree is empty and serializes to nothing",
    "fn-except-node-args-002": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "fn-except-node-args-012": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "fn-except-node-args-013": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "fn-intersect-node-args-015": "helium fn:serialize of a node selected from a larger document does not emit the in-scope namespace declarations the node inherits from its ancestors, producing an undeclared-prefix serialization that omits the expected xmlns declarations",
    "fn-intersect-node-args-016": "helium fn:serialize of a node selected from a larger document does not emit the in-scope namespace declarations the node inherits from its ancestors, producing an undeclared-prefix serialization that omits the expected xmlns declarations",
    "fn-max-14": "helium fn:max returns xs:integer, losing the selected item's derived integer subtype: max((xs:positiveInteger(123),xs:unsignedShort(124))) is not instance of xs:nonNegativeInteger",
    "fn-max-15": "helium fn:max returns xs:integer, losing the selected item's xs:unsignedShort subtype, so assert-type xs:unsignedShort fails",
    "fn-max-16": "helium fn:max does not convert xs:anyURI to xs:string: max((xs:anyURI('http://c.com'),'http://b.com')) stays xs:anyURI, so assert-type xs:string fails",
    "fn-min-14": "helium fn:min returns xs:integer, losing the selected item's derived integer subtype: min((xs:positiveInteger(123),xs:unsignedShort(124))) is not instance of xs:nonNegativeInteger",
    "fn-min-15": "helium fn:min returns xs:integer, losing the selected item's xs:positiveInteger subtype, so assert-type xs:positiveInteger fails",
    "fn-min-16": "helium fn:min does not convert xs:anyURI to xs:string: min((xs:anyURI('http://a.com'),'http://b.com')) stays xs:anyURI, so assert-type xs:string fails",
    "fn-namespace-uri-16": "helium fn:namespace-uri returns xs:string, not xs:anyURI, so assert-type xs:anyURI fails on the empty-string result",
    "fn-union-node-args-001": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "fn-union-node-args-004": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "fn-union-node-args-005": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "fn-union-node-args-006": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "fn-union-node-args-007": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "fn-union-node-args-008": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "fn-union-node-args-009": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "fn-union-node-args-010": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "fn-union-node-args-011": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "fn-union-node-args-012": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "fn-union-node-args-013": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "fn-union-node-args-015": "helium fn:serialize of a node selected from a larger document does not emit the in-scope namespace declarations the node inherits from its ancestors, producing an undeclared-prefix serialization that omits the expected xmlns declarations",
    "fn-union-node-args-016": "helium fn:serialize of a node selected from a larger document does not emit the in-scope namespace declarations the node inherits from its ancestors, producing an undeclared-prefix serialization that omits the expected xmlns declarations",
    "fn-union-node-args-017": "helium fn:serialize of a node selected from a larger document does not emit the in-scope namespace declarations the node inherits from its ancestors, producing an undeclared-prefix serialization that omits the expected xmlns declarations",
    "fo-test-fn-json-to-xml-004": "helium fn:json-to-xml diverges on result attributes (spurious escaped-key/escaped attributes and surrogate-escape handling), so the serialized result differs from the expected",
    "following-12": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "followingsibling-12": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "functx-fn-number-6": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "json-to-xml-011": "helium fn:json-to-xml does not place result elements in the http://www.w3.org/2005/xpath-functions namespace, so the serialized result omits the expected default-namespace declaration",
    "json-to-xml-013": "helium fn:json-to-xml does not place result elements in the http://www.w3.org/2005/xpath-functions namespace, so the serialized result omits the expected default-namespace declaration",
    "json-to-xml-014": "helium fn:json-to-xml does not place result elements in the http://www.w3.org/2005/xpath-functions namespace, so the serialized result omits the expected default-namespace declaration",
    "json-to-xml-016": "requires schemaImport/PSVI type annotation: fn:json-to-xml(...,map{'validate':true()}) should validate against the built-in schema so j:number is typed xs:double, but helium leaves it untyped, so data(...) instance of xs:double is false",
    "json-to-xml-018": "helium fn:json-to-xml drops duplicate JSON object keys, so a repeated key is missing from the result",
    "json-to-xml-021": "helium fn:json-to-xml diverges on result attributes (spurious escaped-key/escaped attributes and surrogate-escape handling), so the serialized result differs from the expected",
    "json-to-xml-024": "helium fn:json-to-xml diverges on result attributes (spurious escaped-key/escaped attributes and surrogate-escape handling), so the serialized result differs from the expected",
    "nametest-12": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "nametest-13": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "op-concatenate-mix-args-020": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "parse-xml-006": "helium fn:serialize emits the source document's DOCTYPE / internal-subset DTD declarations, which the expected assert-xml fragment omits",
    "parse-xml-008": "helium fn:serialize emits the source document's DOCTYPE / internal-subset DTD declarations, which the expected assert-xml fragment omits",
    "parse-xml-009": "helium fn:serialize emits the source document's DOCTYPE / internal-subset DTD declarations, which the expected assert-xml fragment omits",
    "parse-xml-010": "helium fn:serialize emits the source document's DOCTYPE / internal-subset DTD declarations, which the expected assert-xml fragment omits",
    "parse-xml-013": "helium fn:parse-xml of a document with an internal DTD subset produces a result that serializes to the empty string",
    "preceding-12": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "preceding-sibling-12": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "predicates-21": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "predicates-22": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "predicates-24": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "predicates-27": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "predicates-28": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "sequenceexpressionhc8": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "sequenceexpressionhc9": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "string-queries-results-q1": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "unabbreviatedSyntax-1": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "unabbreviatedSyntax-15": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "unabbreviatedSyntax-16": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "unabbreviatedSyntax-18": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "unabbreviatedSyntax-19": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "unabbreviatedSyntax-2": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "unabbreviatedSyntax-23": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "unabbreviatedSyntax-30": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "unabbreviatedSyntax-32": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment",
    "unabbreviatedSyntax-9": "helium fn:serialize inserts a spurious space between adjacent serialized nodes (and between a node and an adjacent atomized value); XSLT/XQuery serialization inserts a separator only between adjacent atomic values, so the serialized $result has extra whitespace versus the expected fragment"
  }
}
